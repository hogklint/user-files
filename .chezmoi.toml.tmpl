{{- $email := promptString "Git config email address" }}
{{- $name := promptString "Git config name" }}
[data]
  email = "{{ $email }}"
  name = "{{ $name }}"
  # False clones with https
  cloneSsh = true
